import express from "express"

//create our MAIN router that will house all of our api routes
const rootRouter = new express.Router();

rootRouter.use("/api/v1/listings",)

export default rootRouter